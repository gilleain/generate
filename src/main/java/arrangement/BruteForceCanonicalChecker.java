package arrangement;

import graph.model.Graph;

/**
 * Checks if an arrangement is canonical by trying all possible cyclic permutations.
 *   
 * @author maclean
 *
 */
public class BruteForceCanonicalChecker {
	
	public static boolean isCanonical(Graph arrangement) {
		int n = arrangement.getVertexCount();
		for (int i = 0; i < n; i++) {
			
		}
		return true;
	}

}
